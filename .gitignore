# Ignore .env files containing sensitive IPs
.env
# Prevent build artifacts and temporary files from being committed
/target/
**/*.rs.bk
Cargo.lock
*.log
*.tmp

# Snyk Security Extension - AI Rules (auto-generated)
.github/instructions/snyk_rules.instructions.md
